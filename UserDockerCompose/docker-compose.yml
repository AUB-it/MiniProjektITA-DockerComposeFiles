services:
  auth-service:
    image: auth-service:0.1.0
    environment:
      - VAULT_URL=https://localhost:8201/
    expose:
      - "8080"
    networks:
      - haavops
    
  user-service:
    image: 
    environment:
      - MONGO_CONNECTION_STRING=mongodb://localhost:27017/
      - MONGO_DATABASE_NAME=UserDb
      - VAULT_URL=https://localhost:8201/
      - AUTHSERVICE_ULR=http://localhost:5294/
    expose:
      - "8080"
    networks:
      - haavuser
  
  nginx:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - user-service
      - auth-service
    ports:
      - "4000:4000"
    networks:
      - haavuser

networks:
  haavuser

  ## nginx goes here, sammen med config filen