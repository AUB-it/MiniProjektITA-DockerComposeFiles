services:


  nginx:
    image: nginx:latest
    volumes:
    - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
    - user-service
    - catalog-service
    - order-service
    ports:
    - "4001:4001"